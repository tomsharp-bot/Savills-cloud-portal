<!DOCTYPE html>
<html lang="en-GB">
<head>
  <meta charset="utf-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <title>Photos — Savills Cloud Portal</title>
  <%- include("partials/head-css") %>
  <link rel="stylesheet" href="<%= baseUrl('/css/programme.css') %>" />
</head>
<body>
<div id="screen-app" class="programme-page">
  <%- include("partials/topbar") %>
  <%- include("partials/admin-hub", { active: "photos" }) %>
  <main class="programme wrap admin-home">
    <div class="head">
      <div>
        <h1>Photos</h1>
        <p class="intro">Shared photo storage is not available on this portal yet. Project documents still live on each project under Project Progress.</p>
      </div>
    </div>
    <p><a class="linkish dark" href="<%= baseUrl('/admin') %>">Back to home</a></p>
  </main>
</div>
</body>
</html>
